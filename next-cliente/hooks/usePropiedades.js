import { React } from "react";
import { styled } from "@emotion/styled";

const usePropiedades = () => {
    const Propiedades = () => {
            <h1>Desde componente Propiedades</h1>
    }
    return {
        Propiedades
    }
}

export default usePropiedades;